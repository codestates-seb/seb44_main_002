spring:
  h2:
    console:
      enabled: true
      settings:
        web-allow-others: true
      path: /h2
  redis:
    host: ${redis_host}
    port: ${redis_port}
  datasource:
    url: jdbc:h2:mem:test
  jpa:
    hibernate:
      ddl-auto: create
    show-sql: true
    properties:
      hibernate:
        format_sql: true
  sql:
    init:
      data-locations: classpath*:db/h2/data.sql
mail:
  address:
    admin: ${mail_address_admin}
jwt:
  key: ${jwt_key}
  access-token-expiration-minutes: 10
  refresh-token-expiration-minutes: 360
cloud:
  aws:
    s3:
      bucket: cocktail-project
    credentials:
      access-key: AKIAUW7LU5KX2L7AKEAU
      secret-key: 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
    region:
      static: ap-northeast-2
      auto: false
    stack:
      auto: false
